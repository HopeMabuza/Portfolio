export default function Manifesto() {
  return (
    <section className="manifesto">
      <p className="manifesto-text">
        The chain doesn't care who you are.<br />
        <em>But your users do.</em><br />
        I write the contracts and the interfaces — so neither side has to compromise.
      </p>
    </section>
  );
}
